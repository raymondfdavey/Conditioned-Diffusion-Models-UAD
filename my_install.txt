(cddpm) rd81@cs31c025338:~/projects/diffusion-uad$ conda create -n cddpm python=3.9
(cddpm) rd81@cs31c025338:~/projects/diffusion-uad$ conda activate cddpm
(cddpm) rd81@cs31c025338:~/projects/diffusion-uad$ conda install pytorch=1.13.1 torchvision torchaudio pytorch-cuda=11.7 -c pytorch -c nvidia
(cddpm) rd81@cs31c025338:~/projects/diffusion-uad$ conda env update -f environment.yml


UPDATE!

so the encironment_working and environment_working_full are more recent versions that I know are working on the linux mmachines in the lab.

i'd use the fullest one if on an actual lab machine, otherwise the no builds on unix

RUN USING:

python run.py experiment=cDDPM/DDPM_cond_spark_2D experiment_name=STILL_SETTING_UP

where this will result in logs in the folliwung:

/home/rd81/projects/experiment_logs/STILL_SETTING_UP_2024-12-05_09-47-34

#! to change:

DDPM_2D line 28

        with open_dict(self.cfg):
            self.cfg['cond_dim'] = cfg.get('unet_dim',128) * 4

IXI datamodeul line 35

  imageDim: [192,192,160] 


ray utils line 269
        'features_raw_embedding': torch.zeros(1, n_slices, 512), 
